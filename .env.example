# Database connection string (PostgreSQL)
DATABASE_URL="postgresql://username:password@localhost:5432/ennead_client_chart?schema=public"

# Optional: Add any other environment variables here later (auth secrets, etc.)